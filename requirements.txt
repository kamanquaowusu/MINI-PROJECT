# Runtime dependencies for serving the SafeMoMo API (training scripts have
# extra deps like pandas -- deliberately not listed; deploys only serve).
# scikit-learn/joblib/numpy/scipy are pinned to the exact versions the model
# bundle was trained with: an unpickled sklearn model is only guaranteed
# valid on the same sklearn version.
fastapi==0.115.12
uvicorn==0.33.0
pydantic==2.10.6
scikit-learn==1.3.2
scipy==1.10.1
joblib==1.4.2
numpy==1.24.4
